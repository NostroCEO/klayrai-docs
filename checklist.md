checklist to run before every launch:

→ Rate limits
→ Row Level Security
→ CAPTCHA on auth + forms
→ Server-side validation
→ API keys secured
→ Env vars set properly
→ CORS restrictions
→ Dependency audit